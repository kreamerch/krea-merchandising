# 📘 Guía rápida: Subir proyecto a GitHub desde CMD

## ✅ Configuración global (una sola vez por PC)

```bash
git config --global user.name "kreamerch"
git config --global user.email "krea.merch@gmail.com"
